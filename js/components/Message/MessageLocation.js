import React, { PropTypes } from "react";

function MessageLocation(props) {
  return (
    <a className="message-location theme-link devtools-monospace">
      //@TODO implement. The existing implementation depends on logic that's
      // hard to pull out of the codebase. 
    </a>
  );
}

export default MessageLocation;
